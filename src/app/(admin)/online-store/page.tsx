import { ComingSoon } from "@/components/admin/coming-soon";

export const metadata = { title: "Online Store" };

export default function Page() {
  return (
    <ComingSoon
      title="Online Store"
      phase="Phase S (Storefront)"
      description="Theme editing, pages, blog posts, and navigation menus arrive with the customer storefront."
    />
  );
}
