import { ComingSoon } from "@/components/admin/coming-soon";

export const metadata = { title: "Price lists" };

export default function Page() {
  return (
    <ComingSoon
      title="Price lists"
      phase="Phase 3"
      description="Custom and volume-based pricing per market, launching together with Markets."
    />
  );
}
