import { ComingSoon } from "@/components/admin/coming-soon";

export const metadata = { title: "Metaobjects" };

export default function Page() {
  return (
    <ComingSoon
      title="Metaobjects"
      phase="Phase 3"
      description="Custom data structures for reusable, structured site content."
    />
  );
}
